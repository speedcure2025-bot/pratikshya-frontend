/**
 * AI Mirror has no seeded presentation looks. Results are available only when
 * a real provider returns an image for an operator-created product.
 */
export const PRODUCT_PREVIEW_OVERRIDES = Object.freeze({});
export const CATEGORY_PREVIEW_SETS = Object.freeze({});
export const getMockTryOnResult = () => null;
export const getMockPreviewTemplate = () => null;
